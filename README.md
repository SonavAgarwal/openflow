# OpenFlow

## Install

```bash
./install.sh
```

This will:
- build the Swift app
- build the transcriber
- download Whisper + VAD models if missing
- bundle models into the app
- install `OpenFlow.app` to `~/Applications`

## Run

Open `~/Applications/OpenFlow.app`.

## Reinstall After Changes

```bash
./install.sh
```
